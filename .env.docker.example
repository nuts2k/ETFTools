# ============================================
# ETFTool Docker 环境变量配置示例
# 使用说明: 复制此文件为 .env.docker 并修改对应值
# cp .env.docker.example .env.docker
# ============================================

# 安全配置（必须修改）
# 生成新密钥: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-at-least-32-characters-long

# 加密 Salt（生产环境强烈建议修改）
# 生成方法: python -c "import secrets; print(secrets.token_urlsafe(16))"
ENCRYPTION_SALT=etftool_telegram_salt

# 速率限制（生产环境建议启用）
ENABLE_RATE_LIMIT=true

# 可选配置（如需自定义可取消注释）
# PROJECT_NAME=ETFTool
# API_V1_STR=/api/v1
# ENVIRONMENT=production
# BACKEND_HOST=127.0.0.1
# BACKEND_PORT=8000
# DATABASE_URL=sqlite:///./etftool.db
# CACHE_DIR=/app/backend/cache
# CACHE_TTL=3600
